{
  "name": "h11",
  "module": "src/h9.ts",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "build": "bun build ./src/h9.ts --outdir ./dist && bun run types",
    "types": "tsc --project tsconfig.types.json"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "utftu": "^1.0.26"
  }
}
